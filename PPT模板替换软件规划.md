# PPT 模板一键替换软件规划

## 1. 结论

这个项目可以做，且适合先做一个“针对固定模板和常见商务 PPT 的高精度替换工具”。

以当前目录中的 3 份文件为例：

- 模板文件：[OpenClaw TVP研讨会-PPT模板.pptx](D:/Desktop/Projects/PPT模板替换/OpenClaw%20TVP研讨会-PPT模板.pptx)
- 源文件 A：[EverMemOS_Introduction.pptx](D:/Desktop/Projects/PPT模板替换/EverMemOS_Introduction.pptx)
- 源文件 B：[手把手带你玩转腾讯版自研小龙虾 WorkBuddy.pptx](D:/Desktop/Projects/PPT模板替换/手把手带你玩转腾讯版自研小龙虾%20WorkBuddy.pptx)

已确认：

- 三份文件都是 16:9。
- 模板中的左上角抬头位置基本固定，适合做规则化套版。
- 源文件背景大多是纯色或简单背景，不是复杂整页拼贴。
- 源文件中既有标准文本框，也有图片、组合对象、表格等混合内容。

因此，项目目标应定义为：

- 第一阶段：一键完成大多数页面的自动替换，并输出低置信度页面供人工复核。
- 不应承诺：任意 PPT、任意模板、100% 无人工检查地完美替换。

## 1.1 当前原型状态

截至 2026-03-24，第一版 CLI + GUI 原型已经落地，相关文件如下：

- GUI 入口：[run_gui.py](D:/Desktop/Projects/PPT模板替换/run_gui.py)
- 双击启动脚本：[launch_gui.bat](D:/Desktop/Projects/PPT模板替换/launch_gui.bat)
- 原型入口：[run_prototype.py](D:/Desktop/Projects/PPT模板替换/run_prototype.py)
- 模板探针入口：[run_template_probe.py](D:/Desktop/Projects/PPT模板替换/run_template_probe.py)
- 核心代码目录：[prototype_tool](D:/Desktop/Projects/PPT模板替换/prototype_tool)
- GUI 模块：[gui.py](D:/Desktop/Projects/PPT模板替换/prototype_tool/gui.py)
- 单元测试：[test_analysis.py](D:/Desktop/Projects/PPT模板替换/tests/test_analysis.py)
- 原型使用说明：[原型使用说明.md](D:/Desktop/Projects/PPT模板替换/原型使用说明.md)
- 验证报告：[验证报告.md](D:/Desktop/Projects/PPT模板替换/validation/prototype/验证报告.md)
- GUI 截图：[gui_preview.png](D:/Desktop/Projects/PPT模板替换/gui_preview.png)

当前原型已经实现：

- 单模板批处理
- 桌面 GUI 入口
- 标题识别
- 封面深色化预览
- 模板母版/版式背景抽取与套用
- 背景替换
- 左上抬头插入
- 左上抬头样式抽取，默认使用模板中的 `腾讯体 W7`
- 当前主题化文本默认统一到 `腾讯体 W7`，同时写入中英文字体槽位
- 模板主题色抽取与自动 palette 生成
- 模板 profile 抽取
- 正文安全区避让
- 局部背景感知的文字换色
- 透明文字容器自动去框，优先保留外层卡片，不再默认给纯文字加内层框
- 长标题和小卡片文字会做规则化缩字号，尽量避免被动换行
- 图表模板化配色
- 表格模板化配色
- 复杂组合对象中的矢量元素主题化
- PowerPoint 原生导出截图
- 风险页报告
- 多文件批量运行
- 实验性 COM 编辑路径和自动回退

当前原型仍然是混合引擎：

- 分析和编辑：`python-pptx`
- 渲染导出：PowerPoint COM

额外说明：

- 已经接入 `--edit-engine com` 实验路径
- 当前在这台机器上，COM 编辑仍可能出现 `RPC server unavailable`
- 因此主流程已加自动回退：COM 失败会落日志并切回稳定编辑路径
- 新增模板探针后，可以先对新模板单独做接入验证，再决定是否批量套用源文件

这意味着它已经足够做产品验证和规则迭代，但还不是最终的高保真生产引擎。后续若要继续提升保真度，编辑层需要逐步迁移到 PowerPoint COM。

## 1.2 模板接入层现状

截至 2026-03-24，模板接入层已经从“只抽 background 和 header”升级到“抽 background + header + palette”。

当前已经支持自动抽取：

- 模板封面/内容页/章节页背景图
- 紧凑/宽版抬头位置、尺寸、颜色
- 模板主背景色
- 模板标题主色
- 模板强调色
- 边框色、面板色、图表色的派生 palette

系统会把这些识别结果落成：

- `template_profile.json`
- `template_palette.json`
- `模板接入报告.md`

当前实测过的第二套模板：

- [TVP七周年-PPT模板.pptx](D:/文档/WXWork/1688856530732420/Cache/File/2025-12/TVP七周年-PPT模板.pptx)
- 探针输出目录：[template_probe_runs/TVP七周年-PPT模板](D:/Desktop/Projects/PPT模板替换/template_probe_runs/TVP七周年-PPT模板)

结论是：这套接入层已经足以覆盖同家族 TVP 深色模板，但还不能宣称“任意模板通用”。

## 2. 产品目标

用户输入：

- 1 个目标模板 PPT
- 1 个或多个待替换 PPT

系统输出：

- 1 个按模板风格重构后的 PPT
- 1 份替换报告
- 1 份低置信度页清单

核心要求：

- 封面替换
- 每页背景替换
- 识别每页标题并套用模板左上角抬头
- 保留正文内容和基本版式
- 根据背景明暗自动调整文字颜色
- 尽可能保持字体、层级、对齐、间距和可编辑性

## 3. 最重要的四件事

这类软件成败主要取决于 4 个维度：

### 3.1 版式

版式优先级最高。正文不只是“文字搬过去”，而是要保住：

- 标题与正文的相对层级
- 主要内容块的位置关系
- 左右分栏、卡片、时间线、对比区等结构
- 页边距、安全区、顶部抬头占位
- 图片与文字的相对关系

结论：

- 不能只做“文本抽取 + 重新排版”。
- 必须优先保留原页内容块的几何关系，再叠加模板元素。

### 3.2 字体

字体决定观感是否像“同一份 PPT”。

当前样本中已出现：

- 模板：`腾讯体 W7`、`腾讯体 W3`
- 源文件：`Calibri`、`Georgia`、`Consolas`
- 源文件：`Microsoft YaHei`、`TencentSans W7`、`TencentSans W3`、`方正兰亭` 等

风险点：

- 目标机器未安装模板字体会发生替换，导致行高、换行、溢出全部变化。
- 中英文字体混排时，局部 run 级别字体可能不一致。
- 图片内文字无法按字体级别修正。

结论：

- 字体必须纳入“强约束”，不能作为后处理小问题。
- 需要做字体依赖检查、缺失字体预警、替代策略和溢出复检。

### 3.3 背景

背景不是单纯换颜色，实际上包含：

- 幻灯片背景色
- 母版背景
- 页眉装饰
- 顶部抬头槽位
- 页脚装饰线、品牌标识、角标

结论：

- 最稳的方式不是逐页“涂白/涂黑”，而是基于模板页或模板母版重新套入背景层。
- 背景替换应先于正文颜色替换。

### 3.4 颜色

颜色处理至少要覆盖：

- 标题颜色
- 正文字色
- 强调色
- 图表颜色
- 线框、图标、标签底色

结论：

- 不能只做“浅底黑字 / 深底白字”的二元替换。
- 需要一套模板颜色映射规则，至少区分：
  - 标题主色
  - 正文字色
  - 次级说明色
  - 强调色
  - 警示/高亮色

## 4. 推荐的实现思路

推荐采用“规则引擎 + PPT 原生编辑引擎 + 多模态模型复核 + QA 校验”四段式架构。

### 4.1 处理总流程

1. 读取模板和源 PPT
2. 抽取每页结构信息
3. 识别页类型
4. 替换封面和背景
5. 识别标题并映射到模板左上抬头
6. 搬运正文内容块
7. 根据背景和模板主题调整颜色
8. 渲染整份 PPT 为图片
9. 做视觉 QA 和低置信度标记
10. 输出成品和报告

### 4.2 每页的数据抽取

每页至少抽取：

- 页面尺寸
- 归一化坐标系
- 背景色/背景类型
- 文本框位置、宽高、层级
- 每个文本框的字号、字体、粗细、颜色
- 图片位置
- 图片裁切、旋转、透明度
- 表格位置
- 图表对象
- 组合对象和 SmartArt
- Z-Order 层级顺序
- 是否存在全页大标题
- 是否存在左上角可映射为抬头的标题候选

### 4.3 页类型识别

先做规则识别，再让视觉模型复核。

页类型建议至少分为：

- 封面页
- 目录页
- 章节过渡页
- 普通正文页
- 图文排版页
- 表格页
- 图表页
- 大图页
- Q&A / Thank You 页

原因：

- 不同页型的标题识别规则不同。
- 不同页型的背景与颜色策略不同。
- 不同页型的正文搬运策略也不同。

### 4.4 标题识别策略

标题识别不能完全交给大模型，最稳的是“两阶段”：

第一阶段：规则候选

- 位置靠上
- 字号最大或次大
- 字重更粗
- 文本较短
- 位于页面主要内容块上方

第二阶段：视觉模型复核

- 对整页截图做判断
- 在候选中选择真正标题
- 输出置信度

低置信度条件：

- 同页存在两个以上大标题候选
- 标题位于非标准区域
- 页面是目录/章节/大字封面样式
- 页内是时间线或多列卡片，多个区块都有 header

### 4.5 正文搬运策略

正文“原封不动”不能理解为逐字符复制到新模板位置，否则会破坏版式。

应分三层处理：

- 第一层：保留原对象位置和尺寸
- 第二层：清理与模板冲突的区域
- 第三层：只对颜色、字体映射、顶部安全区做必要调整

建议规则：

- 标题对象从原页移除或隐藏，避免与模板抬头重复
- 正文对象尽量原位保留
- 若正文侵入模板左上抬头安全区，则整体下移或缩放
- 图片尽量不重采样，不重新截图
- 表格优先保留原表格对象
- 图表优先原样保留；如果图表配色与背景冲突，再做颜色修正

### 4.6 颜色与对比度策略

必须做自动对比度检查。

建议：

- 先判定页面主背景是浅色还是深色
- 再对每个文本 run 计算对比度
- 低于阈值的文本进入颜色修正

颜色修正优先级：

1. 标题使用模板标题色
2. 正文使用模板正文色
3. 注释使用模板次级色
4. 原本是强调色的内容映射到模板强调色

注意：

- 不要一刀切把所有正文都改成纯黑或纯白
- 原页如果已有品牌色强调，需要保留“强调关系”

## 5. 技术路线对比

这里有 4 条技术路线。

### 5.1 路线 A：`python-pptx` 直接改

优点：

- 开发快
- 纯 Python
- 易做原型

缺点：

- 对复杂对象支持弱
- 容易丢失格式细节
- 对母版、图表、组合对象、动画支持不足
- 高保真不够

结论：

- 只适合做分析工具或低保真原型
- 不适合作为最终高精度生产引擎

### 5.2 路线 B：解包 PPTX 后直接改 XML

优点：

- 控制力强
- 某些模板替换操作非常精准

缺点：

- 工程复杂度高
- 维护成本高
- 稍有不慎就损坏 PPT
- 对不同对象模型的兼容性很痛苦

结论：

- 可作为局部补刀工具
- 不建议作为主引擎

### 5.3 路线 C：PowerPoint COM 自动化

优点：

- 保真最高
- 可以直接调用 PowerPoint 原生对象模型
- 对母版、布局、文本框、图表、表格等支持最好
- 适合 Windows 本地工具

缺点：

- 依赖本机安装 PowerPoint
- 仅适合 Windows
- 需要处理 COM 稳定性、进程释放、异常恢复

结论：

- 这是第一阶段最推荐的主路线

### 5.4 路线 D：PowerPoint 插件

可分两种：

- Office.js Web Add-in
- VSTO / COM Add-in

Office.js 优点：

- 跨平台一些
- UI 集成到 PowerPoint 中

Office.js 缺点：

- 幻灯片底层控制能力有限
- 高精度页面重构不如原生 COM
- 很多复杂对象处理不够直接

VSTO / COM Add-in 优点：

- 深度集成 PowerPoint
- 能直接操作 PowerPoint 原生对象模型

VSTO / COM Add-in 缺点：

- 开发、部署、签名、兼容性复杂
- 插件分发和升级麻烦

结论：

- 如果目标是尽快做出高精度可用版本，优先做本地 Windows 工具，不先做插件。
- 等核心引擎稳定后，再考虑把能力包装成插件。

## 6. 推荐产品形态

### 6.1 第一阶段推荐

做一个本地 Windows 工具，形态可以是：

- CLI 命令行工具
- 简单桌面界面
- 本地 Web 界面

当前状态：

- CLI 已落地
- 简单桌面界面已落地
- 当前不建议优先走本地 Web 界面

推荐原因：

- 能直接调用 PowerPoint COM
- 便于批量处理
- 开发速度快
- 更适合调试复杂版式问题

### 6.2 第二阶段可选

在引擎稳定后，再封装为 PowerPoint 插件：

- 在 PPT 内选择模板
- 选择要处理的文件
- 直接预览低置信度页
- 一键回写结果

## 7. MVP 范围

第一版只做最能打的功能，不追求全覆盖。

### 7.1 必做

- 单模板替换
- 单文件输入
- 封面替换
- 每页背景替换
- 左上抬头自动套入
- 正文和图片尽量原位保留
- 明暗背景下文字自动换色
- 输出低置信度页清单
- 导出替换前后缩略图对比

### 7.2 暂不承诺

- 动画完整迁移
- 切页转场完全保留
- 音视频对象完整处理
- 任意 SmartArt 的完美编辑级迁移
- 扫描截图里的文字自动重排
- 任意模板之间完全通用

## 8. 高精度的关键机制

要做到“尽可能高精度”，必须加入以下机制。

### 8.1 安全区机制

模板左上抬头会占用固定区域。

因此每页都要定义：

- 顶部安全区
- 左上抬头保留区
- 页脚保留区

如果正文对象侵入这些区域，需要：

- 整体下移
- 缩小
- 或标记为低置信度页

### 8.2 字体依赖检查

处理前先检查：

- 模板字体是否安装
- 源文件字体是否安装
- 是否发生字体替代

如果缺失：

- 记录告警
- 使用预设替代字体
- 对受影响页面做溢出检测

### 8.3 视觉 QA

不能只相信对象层数据，必须渲染后检查：

- 文本是否溢出
- 颜色是否看不清
- 标题是否重复
- 顶部抬头是否与正文打架
- 背景是否覆盖不完整

### 8.4 低置信度策略

以下情况必须自动标红：

- 标题识别置信度低
- 页面发生明显位移/缩放
- 文字溢出
- 多对象重叠
- 图表颜色对比不足
- 字体替代导致换行变化
- 复杂组合对象无法拆解

## 9. 容易遗漏但必须纳入规划的点

这是本项目最容易漏掉、但会直接影响结果的部分。

### 9.1 母版与版式页

不能只看普通幻灯片内容，还要处理：

- Slide Master
- Slide Layout
- Theme

否则：

- 背景可能替换不完整
- 模板字体可能不生效
- 默认颜色可能不一致

### 9.2 run 级别格式

一段文字内部可能同时有：

- 加粗
- 颜色强调
- 超链接
- 中英文不同字体

如果只按文本框级别替换，会丢细节。

### 9.3 图表与表格

图表和表格不能按普通文本框处理。

要单独考虑：

- 图表标签颜色
- 坐标轴文字颜色
- 表格边框颜色
- 表头背景色

### 9.4 图片中的文字

截图、海报、流程图图片中的文字不能靠对象级修正。

因此：

- 只可做整体明暗冲突检测
- 无法保证图片内文字永远可读
- 需要将这类页面列为潜在复核页

### 9.5 动画、批注、备注、超链接

如果用户最终在意这些内容，必须明确范围：

- 是否保留动画
- 是否保留讲稿备注
- 是否保留批注
- 是否保留超链接和动作

第一版建议：

- 尽量保留备注和超链接
- 不承诺复杂动画完整迁移

### 9.6 字体授权

模板中的腾讯体、TencentSans、方正系字体可能涉及授权和安装环境。

要在产品里明确：

- 需要用户本机已有授权字体
- 或者内置字体检测与提示

### 9.7 坐标缩放与页面尺寸归一化

当前样本虽然都是 16:9，但实际尺寸并不相同：

- 10 x 5.625
- 13.33 x 7.5
- 20 x 11.25

这意味着：

- 不能直接拿源页的绝对坐标写入模板页
- 必须先映射到统一比例坐标系
- 需要区分“等比缩放后保留位置”与“按模板安全区重新布局”

否则最容易出现：

- 标题位置偏移
- 正文被抬头盖住
- 页脚元素上浮

### 9.8 图层顺序、裁切和旋转

很多 PPT 看起来问题不大，但其实错误发生在图层层级：

- 背景被插到了正文上层
- 图片遮住标题
- 卡片底色跑到文字上面
- 裁切过的图片恢复成原图比例
- 旋转对象角度丢失

因此在对象迁移时，必须保留：

- Z-Order 顺序
- 图片 crop 参数
- 旋转角度
- 透明度和阴影等关键视觉参数

## 10. 推荐架构

### 10.1 核心模块

- `ingest`：读取 PPT、模板、主题信息
- `analyzer`：抽取每页对象树和样式信息
- `classifier`：识别页类型和标题候选
- `mapper`：模板槽位映射
- `rewriter`：执行背景、抬头、颜色、对象迁移
- `qa`：渲染检查与低置信度标记
- `reporter`：输出报告和缩略图

### 10.2 推荐技术栈

- 主引擎：Python + `pywin32` + PowerPoint COM
- 页面分析：`python-pptx` 作为辅助读取
- 视觉复核：多模态模型 API
- QA 渲染：PowerPoint 导出 PDF/图片
- UI：第一版可用 `Tkinter`、`PySide6` 或简单本地 Web

## 11. 开发阶段建议

### 阶段 1：验证可行性

- 跑通模板读取
- 跑通单页背景替换
- 跑通标题识别
- 跑通正文搬运
- 跑通文字颜色对比修正

验收标准：

- 至少 2 份样本 PPT 可以自动处理
- 70% 以上页面无需人工改动
- 低置信度页能正确标出

### 阶段 2：MVP

- 支持完整单份 PPT 批处理
- 输出报告
- 输出替换结果预览
- 支持模板参数配置

验收标准：

- 80% 以上页面直接可用
- 低置信度页召回率高

### 阶段 3：产品化

- 批量处理多个 PPT
- 模板库
- 配色规则配置
- 人工校正回写
- 插件形态封装

## 12. 我的明确建议

如果以“高精度”为第一目标，我的建议是：

- 先做本地 Windows 工具，不先做插件
- 主引擎用 PowerPoint COM，不用 `python-pptx` 直接硬改
- 标题识别采用“规则候选 + 多模态复核”
- 结果必须带低置信度页和 QA 报告
- 第一版只支持少量固定模板，不追求任意模板通用

原因很直接：

- 这样最快做出真正能用的版本
- 保真度最高
- 调试成本最低
- 最适合你现在这个明确模板替换场景

## 13. 自检清单

这份规划已经覆盖以下关键项：

- 封面替换
- 背景替换
- 标题识别
- 左上抬头套版
- 正文搬运
- 字体处理
- 配色和对比度
- 版式保留
- 图表/表格处理
- 低置信度页
- QA 渲染复核
- 本地工具与插件路线对比
- 第一阶段与后续阶段划分

仍需在真正开始开发前确认的事项：

- 模板是否只有这一套，还是未来有多套
- 是否必须保留动画
- 是否必须保留备注/讲稿
- 是否接受依赖本机安装 PowerPoint
- 是否接受依赖模板字体安装
- 低置信度页是否允许人工点选修正

## 14. 当前进展与下一步

已经完成的验证：

1. 已验证模板场景下的标题识别可行
2. 已验证封面、背景、抬头、安全区、字色修正可以串起来执行
3. 已验证两份源 PPT 可以批量处理并输出报告
4. 已验证复杂页能够被正确标为风险页，而不是直接静默失败

当前最合理的下一步：

1. 把编辑层逐步从 `python-pptx` 迁移到 PowerPoint COM
2. 把模板 profile 从当前的 header/safe-zone 抽取，扩展到更多模板元素
3. 增强图表、表格、组合对象和图片裁切保真
4. 增加低置信度页的人工校正入口
5. 继续迭代桌面 GUI，等引擎稳定后再考虑封装成插件
